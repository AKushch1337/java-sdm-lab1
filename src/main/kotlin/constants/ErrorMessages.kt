package constants

enum class ErrorMessages(val message: String) {
    A_GREATER("a is greater than n"),
    B_GREATER("b is greater than m"),
    DENOMINATOR_EQUALS_ZERO("denominator equals 0"),
    B_LESS_THAN_ZERO("b is less than 0")
}